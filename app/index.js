import 'whatwg-fetch';
//import 'data.json';
//import addGamesToList from `./twitch/add-games-to-list`;

// Look up a `ul` element with a class called `results`
// Add games to the results list based on the data from `data.json`




//const grid = document.querySelector('.grid');
//const gridItem = document.createElement('div');


/*gridItem.classList.add('grid__item');
gridItem.innerHTML = `
  <div class="grid__item">
    <img class="photo">
    <a class="title"></a>
    <p class="views"></p>
  </div>`;


.gridItem.querySelector('.photo').innerText = game.box.medium;
.gridItem.querySelector('.title').innerText = game.name;
.gridItem.querySelector('.views').innerText = game.viewers;

  return gridItem;
}


// for (let i = 0; i < gameList.length; i++) {
//   const game = gameList[i];
gameList.forEach((game) => {
  const profileElement = createProfileElement(game);

  // Add the profileElement to the end of the "grid" element
  grid.appendChild(profileElement);
});
*/
